
// Definição das variáveis
const INDICE = 13;
let soma = 0;
let k = 0;

// Loop 'while' que soma os números de 1 até INDICE
while (k < INDICE) {
    k += 1;
    soma += k;
}

// Impressão do resultado
console.log(`O valor da variável SOMA ao final do processamento é: ${soma}`); //91
